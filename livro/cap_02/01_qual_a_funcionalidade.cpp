/*
    (a) int x;
    R: declara um inteiro chamado x

    (b) float salario = 1000.0;
    R: declara um número real chamado salario e atribui a ele o valor de 1000.0

    (c) char c = ‘z’;
    R: declara o carácter c e atribui o valor de 'z'

    (d) bool achou = false;
    R: declara a variável do tipo booleana chamada achou e atribui a ela o valor de falso

    (e) float X, Y, Z;
    R: declara três variáveis do tipo real chamadas X, Y e Z respectivamente

    (f) int a = 1, b = 2;
    R: declara duas variáveis do tipo inteiro, chamadas de a e b e atribui os valores de 1 e 2 respectivamente

    (g) cout << "Programacao" << endl;
    R: Imprime na tela a palavra Programacao

    (h) cin >> tamanho;
    R: Recebe do usuário do programa o valor da variável tamanho, que tem tipo desconhecido

    (i) cout << "Eu tenho " << anos << "de idade\n";
    R: Imprime na tela a frase "Eu tenho", seguido da variavel anos, e depois " de idade", ou seja, imprime na tela "Eu tenho x anos de idade", sendo x a variável anos, que provavelmente contém um número com a idade.
    
    (j) cin >> X >> Y >> Z;
    R: Recebe do usuário do programa o valor das três variáveis X, Y e Z, nesta ordem.

    (k) A = 17%5;
    R: A variável A recebe o resto da divisão de 17 por 5

    (l) media = (n1 + n2 + n3 + n4)/4;
    R: A variável media recebe a soma de outras 4 variáveis divido por 4

    (m) j = j + 1;
    R: A variavel j recebe o valor dela mesmo mais 1

    (n) j++;
    R: j é incrementado em 1, ou seja recebe o valor de j+1

    (o) a > b;
    R: testa se a variável a é maior do que a variável b e retorna o valor de 1, se verdadeiro, ou 0, se falso

    (p) a || b;
    R: Testa se pelo menos um dos valores é verdadeiro e retorna 1, se verdadeiro, ou 0, se falso

    (q) a <= x && x <= b;
    R: Testa se as duas comparações são, ao mesmo tempo, verdadeiras e retorna 1, se verdadeiro, ou 0, se falso

    (r) s == ‘m’ || s == ‘M’;
    R: Testa se a variável s é igual a m (minúsculo) ou se s é igual a M (maiúsculo), retorna 1, se pelo uma das condições é verdadeira, ou 0, se nenhuma delas é verdadeira.

    (s) a = b == c;
    R: A variável a recebe o valor booleano da comparação da variável b igual a variável c, ou seja, se elas forem iguais, a recebe 1, se elas forem diferente a recebe 0
*/